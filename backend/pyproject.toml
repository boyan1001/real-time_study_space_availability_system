[project]
name = "backend"
version = "0.1.0"
description = "QR Code Door Security System Backend"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.136.1",
    "uvicorn[standard]>=0.47.0",
    "python-dotenv>=1.0.0",
]
